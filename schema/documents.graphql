type GraphDocument {
     id: ID!
     title: String!
     description: String!
     file: String!
}

input documentInput {
      id: ID!
      title: String!
      description: String!
      file: String!
}

type mutation {
     createDocument(input: documentInput!): GraphDocument!
}