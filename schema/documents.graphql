type GraphDocument {
     id: ID!
     title: String!
     description: String!
     file: String!
}

input DocumentInput {
      title: String!
      description: String!
      file: String!
}

extend type Query {
     docs: [GraphDocument!]!
}

extend type Mutation {
     createDocument(input: DocumentInput!): GraphDocument!
}