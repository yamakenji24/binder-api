type User {
     id: ID!
     username: String!
     password: String!
     email: String!
}

input LoginInput {
      username: String!
      password: String!
}

type Token {
     jwt: String!
}

type Query {
     user: User!
}

type Mutation {
     login(input: LoginInput!): String!
}
